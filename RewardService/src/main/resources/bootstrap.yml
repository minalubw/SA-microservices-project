spring:
    config:
        import:
            optional:configserver:http://localhost:8888
    application:
        name: RewardService
server:
    port: 7076

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/

