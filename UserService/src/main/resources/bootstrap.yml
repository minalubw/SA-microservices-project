spring:
    config:
        import:
            optional:configserver:http://localhost:8888
    application:
        name: UserService
server:
    port: 7070

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
