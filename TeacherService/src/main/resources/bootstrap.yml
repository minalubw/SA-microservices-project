spring:
  config:
    import:
      optional:configserver:http://localhost:8888

  application:
     name: TeacherServices
server:
  port: 7072

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


